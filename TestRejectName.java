package Grupo4;
/*
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class TestRejectName {
    // public RejectName(String nm) {this.name = nm; }

    @Test
    public void testConstruct() {
        RejectionRule rule = new RejectName("Joana");
        assertEquals("Reject name Joana", rule.getDescription());

    }

    @Test
    public void testReject() {
        RejectionRule rule = new RejectName("Joana");
        assertFalse( rule.reject( new SocialUser("João", "Jo")) );

        User u = new SocialUser("Joana", "Jo");
        assertTrue( rule.reject(u) );
        rule = new RejectName("JO");
        assertTrue( rule.reject( u ) );
    }

}
*/