package Grupo4;
/*
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class TestRejectDate {
   //public RejectName(String nm) {this.name = nm; }

    @Test
    public void testConstruct() {
        Date dt = new Date( 5, 10, 2020);
        RejectionRule rule = new RejectDate(dt);
        assertEquals("Creation date less than " + dt, rule.getDescription());
    }

    @Test
    public void testReject() {
        Date dt = new Date( 5, 10, 2020);
        RejectionRule rule = new RejectDate(dt);
        User u = new SocialUser("João", "Jo");
        assertFalse( rule.reject( u ) );
        dt = new Date( 5, 10, u.creationDate.year +1);
        rule = new RejectDate(dt);
        assertTrue( rule.reject( u ) );
    }


}
*/